如果希望ssh登录，请上传密钥到~/.ssh 文件夹中，然后
    cd ~/.ssh
    cat id_rsa.pub > authorized_keys

如果希望启动JupyterLab服务，执行
    /root/run.sh jupyterlab

如果希望启动SSH服务，执行
    /root/run.sh sshd
    /root/run.sh sshd -D

如果希望同时启动SSH服务+JupyterLab服务，执行
    /root/run.sh

